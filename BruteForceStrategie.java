public class BruteForceStrategie implements StrategieFactory {
    public void execute(String login, cibleFactory target) {
        // Générer toutes les combinaisons et tester target.authenticate(...)
    }
}
